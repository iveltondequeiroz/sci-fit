/*	const mappedStudents = this.state.students.map(
			student =>
				<ListItem
					primaryText={student.name}
					leftAvatar={<Avatar src={student.img}/>}
					onClick={this.setUser1.bind(this)}
				/>
		)
	*/	